diff --git a/lib/paseo/packages/server/dist/server/server/web-ui.js b/lib/paseo/packages/server/dist/server/server/web-ui.js
--- a/lib/paseo/packages/server/dist/server/server/web-ui.js
+++ b/lib/paseo/packages/server/dist/server/server/web-ui.js
@@ -190,8 +190,12 @@ function serializeInlineScriptJson(value) {
 function injectConnectionHint(html, req, label) {
     const host = typeof req.headers.host === "string" ? req.headers.host : "";
     const useTls = req.protocol === "https";
+    const endpoint = URL.parse(`${useTls ? "https" : "http"}://${host}`);
+    const listen = endpoint
+        ? `${endpoint.hostname}:${endpoint.port || (useTls ? "443" : "80")}`
+        : host;
     const hint = {
-        listen: host,
+        listen,
         useTls,
         label,
     };
